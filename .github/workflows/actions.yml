name: check-audio

on:
    - push

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Audio interfaces
              run: |
                sudo apt -y install alsa
                aplay -l
